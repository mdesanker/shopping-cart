import styled from "styled-components";

const Contact = () => {
  return (
    <ContactContainer>
      <h1>Contact page</h1>
    </ContactContainer>
  );
};

const ContactContainer = styled.section``;

export default Contact;
